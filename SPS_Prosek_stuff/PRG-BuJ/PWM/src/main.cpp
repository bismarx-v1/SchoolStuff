#include <Arduino.h>
#define GREEN 10
#define RED 11


void setup() {
  pinMode(GREEN, OUTPUT);
  pinMode(RED, OUTPUT);
}

//from 0 to 255 loop
/*void loop() {
  for(int i = 0; i <= 255; i++) {
    analogWrite(RED, i);
    delay(2);
  }
  for(int i = 255; i >= 0; i--) {
    analogWrite(RED, i);
    delay(2);

  for(int i = 0; i <= 255; i++) {
    analogWrite(GREEN, i);
    delay(2);
  }
  for(int i = 255; i >= 0; i--) {
    analogWrite(GREEN, i);
    delay(2);
  }
  }
}*/

void loop() {
  sin(t)
}